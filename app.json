{
  "name": "export-wins-ui-mi",
  "stack": "heroku-16",
  "env": {
    "NODE_MODULES_CACHE": {
      "description": "Set to false so the build works",
      "value": false
    },
    "NPM_CONFIG_PRODUCTION": {
      "description": "Set to false so the build works",
      "value": false
    }
  },
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-nodejs"
    }
  ]
}
